let container = document.querySelector('.container-slider')

// slides = document.querySelectorAll(".images");
let images = ["sandwich.jpg", "salmon.jpg", "cherries.jpg"]

images.forEach(function (image)){
    // Créer une balise image
    // Remplir l'attribut src de la balise créée
    // Insérer la balise créée dans container

    let monImage = document.createElement('img');

    let monImage = "sandwich.jpg";
    let container = document.getElementById(".container-slider");
    container.innerHTML = '';

    maDiv.appendChild(images);


}

